% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/experiment_utils.R
\name{start_experiment}
\alias{start_experiment}
\title{Start a new experiment}
\usage{
start_experiment(
  name,
  prompt_version_id,
  model,
  dataset_name = NULL,
  notes = NULL,
  conn = NULL,
  db_path = "llm_results.db"
)
}
\arguments{
\item{name}{Experiment name (e.g., "baseline_test", "keyword_enhanced_v2")}

\item{prompt_version_id}{ID of the prompt version to use}

\item{model}{Model identifier (e.g., "gpt-4", "claude-3")}

\item{dataset_name}{Optional dataset identifier}

\item{notes}{Optional notes about this experiment}

\item{conn}{Database connection (optional)}

\item{db_path}{Path to database file}
}
\value{
Experiment ID or NULL if failed
}
\description{
Creates a new experiment record for tracking a batch of tests.
}
\examples{
\dontrun{
# Start a baseline experiment
prompt_id <- register_prompt("System prompt", "User template")

exp_id <- start_experiment(
  name = "baseline_test_jan2025",
  prompt_version_id = prompt_id,
  model = "gpt-4o-mini",
  dataset_name = "NVDRS_sample_100",
  notes = "Testing baseline prompt on 100 random cases"
)

# Run your tests and store results
# ... process narratives ...
# store_experiment_result(exp_id, narrative_id, parsed_result)

# Complete the experiment
complete_experiment(exp_id)

# View experiment summary
experiments <- list_experiments()
print(experiments[experiments$id == exp_id, ])
}
}
