experiment:
  name: "test_minimal"

model:
  name: "test-model"
  temperature: 0.0
  api_url: "http://localhost:1234/v1/chat/completions"

prompt:
  version: "test-v1"
  system_prompt: "You are an IPV detection assistant."
  user_template: "Analyze this narrative for IPV: <<TEXT>>"

data:
  file: "tests/fixtures/data/test_data.csv"

run:
  seed: 123
  save_incremental: true
  save_csv_json: true
