# PostgreSQL Environment Configuration
# Copy this file to .env in your project root and update with your settings
# DO NOT commit .env file to version control - it contains sensitive information

# PostgreSQL Connection Settings (REQUIRED)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=ipv_detection
POSTGRES_USER=ipv_user
POSTGRES_PASSWORD=your-secure-password-here

# Optional PostgreSQL Settings
POSTGRES_TIMEOUT=10
POSTGRES_SSL=false
POSTGRES_RETRY_ATTEMPTS=3

# API Configuration (if using external LLM services)
# Uncomment and set the appropriate API key for your service
# OPENAI_API_KEY=your-openai-api-key-here
# ANTHROPIC_API_KEY=your-anthropic-api-key-here
# GOOGLE_API_KEY=your-google-api-key-here

# Application Settings
# LOG_LEVEL=INFO
# ENABLE_PERFORMANCE_MONITORING=true
# BENCHMARK_ON_STARTUP=false

# Security Settings (Production)
# POSTGRES_SSL=true
# POSTGRES_SSL_MODE=require
# POSTGRES_SSL_CERT=/path/to/client-cert.pem
# POSTGRES_SSL_KEY=/path/to/client-key.pem
# POSTGRES_SSL_ROOT_CERT=/path/to/ca-cert.pem

# Example Production Configuration
# POSTGRES_HOST=prod-postgres.your-domain.com
# POSTGRES_PORT=5432
# POSTGRES_DB=ipv_detection_prod
# POSTGRES_USER=ipv_prod_user
# POSTGRES_PASSWORD=very-secure-production-password
# POSTGRES_SSL=true
# POSTGRES_SSL_MODE=require